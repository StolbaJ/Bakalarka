.idea

# Environment variables
.env
.env.local
.env.development
.env.production

.DS_Store

# application.yml je v repu – neobsahuje hesla, jen ${VAR} a výchozí hodnoty pro lokál.
# Skutečná hesla (JWT_SECRET, DB_PASSWORD) se dávají přes env / GitHub Secrets.

package-lock.json